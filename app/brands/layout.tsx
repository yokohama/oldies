export const metadata = {
  title: "ブランド一覧 | Oldies",
  description: "Oldiesのブランド一覧ページです。",
};

export default function BrandsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
