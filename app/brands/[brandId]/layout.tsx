export const metadata = {
  title: "ブランド詳細 | Oldies",
  description: "Oldiesのブランド詳細ページです。",
};

export default function BrandLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
